# Program1C
# Nicholas Arvo
# Purpose:  This program prompts the user for their age and name and
#           then uses those values in a subsequent print statement.

name = input("Enter your name: ")
age = int(input("Enter your age: "))
print(name, "is", age, "years old.")
